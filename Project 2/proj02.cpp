#include<iostream>
#include<iomanip>

using namespace std;

int find_longest(/* dictionary with value and sequence */){

int longest = 0;

//cycle
//check vs variable
//replace if longer
//

return /* value , sequence */;

}

int find_largest(/* dictionary with value and sequence */) {

int largest = 0;

//cycle
//check vs variable
//replace if larger

return /* value , sequence */;

}

int main(){

//get two intergers: a low value followed by a high value
	
	cout << "Enter two intergers, a low value followed by a high value";
	
	int ceiling_int;
	int floor_int;

	cout << "Enter an interger to be used as a floor value: ";
	cin >> ceiling_int;

	count << "Enter an interger to be used as a ceiling value (must be higher than the floor value): ";
	cin >> floor_int;
    // if high value < low, return error and quit
	while (ceiling_int < floor_int) {

		cout << "Error: Ceiling value is less than Floor value. Please enter valid values."

		cout << "Enter an interger to be used as a floor value: ";
		cin >> ceiling_int;

		count << "Enter an interger to be used as a ceiling value (must be higher than the floor value): ";
		cin >> floor_int;

	}
//thats a range
	std::map<int, std::double> sequence_dict;
	
//generate Juggler sequence for each value in range and return 
	for (int a = floor_int, a < ceiling_int, a += 1) {

		// if the int is even
		if (a % 2 = 0) {

			double juggler_double = (a) ^ (1 / 2);

		}
		else {

			double juggler_double = (a) ^ (3 / 2);

		}

	}
//if high >= low, print:
    //line 1, sequence of the longest length, text = starting number [space] length of sequence
    //line 2, sequence that genereated the largest number, text = starting number [space] largest number generated




}
